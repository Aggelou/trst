apply plugin: "war"

bootWar {
    mainClassName = "com.soft.trst.TrstApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
